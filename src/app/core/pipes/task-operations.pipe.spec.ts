import { TaskOperationsPipe } from './task-operations.pipe';

describe('TaskOperationsPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskOperationsPipe();
    expect(pipe).toBeTruthy();
  });
});
